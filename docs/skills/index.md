# Skills（用例・手順）

`.claude/skills/*/SKILL.md` をサイトに取り込んだもの。各ページは SKILL.md を
単一ソースとして transclude している。

- 参照系: `code-quality` / `design-patterns` / `security` /
  `test-code-quality` / `unit-testing-strategy`
- 実行系（`disable-model-invocation`）: `lint-fix` / `run-tests` /
  `create-migration`
